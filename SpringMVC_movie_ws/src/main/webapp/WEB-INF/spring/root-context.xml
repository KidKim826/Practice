<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:mybatis-spring="http://mybatis.org/schema/mybatis-spring"
	xsi:schemaLocation="http://mybatis.org/schema/mybatis-spring http://mybatis.org/schema/mybatis-spring-1.2.xsd
		http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.3.xsd">

	<!-- Root Context: defines shared resources visible to all other web components -->
	
	<!-- com.ssafy.ws.model 패키지를 스캔해서 등록 -->
	<context:component-scan base-package="com.ssafy.ws.model.service, com.ssafy.ws.model.repo"/>
	

	<bean class="org.springframework.jdbc.datasource.DriverManagerDataSource" id="dataSource"> <!-- datasource 는 연결통로 -->
		<property name="driverClassName" value="com.mysql.cj.jdbc.Driver"></property>
		<property name="url" value="jdbc:mysql://localhost:3306/ssafy_movie?serverTimezone=UTC"></property>
		<property name="username" value="ssafy"></property>
		<property name="password" value="ssafy"></property>
	</bean>

	<!-- 무비다오를 스프링이 하나 갖고 있고 무비다오 하고 잇으면 autowired하고 있으면 먹여줘 -->
	<bean class="org.mybatis.spring.mapper.MapperFactoryBean">
		<property name="sqlSessionFactory" ref="sqlSessionFactory"></property>
		<property name="mapperInterface" value="com.ssafy.ws.MovieRepo"></property>
	</bean>

	<!-- 1. transactionManager를 dataSource를 먹여서 생성해서 빈으로 등록. -->
	<!-- 트랜잭션 처리를 위한 transactionManager등록/ / 아이디 값은 고정! -->
	<bean class="org.springframework.jdbc.datasource.DataSourceTransactionManager" id="transactionManager">
		<!-- <property name="dataSource" ref="dataSource"></property> -->
		<constructor-arg ref="dataSource"></constructor-arg>
	</bean>



	<!-- SqlSessionFactory를 빈으로 등록하자. -->
	<bean class="org.mybatis.spring.SqlSessionFactoryBean" id="sqlSessionFactory">
		<property name="dataSource" ref="dataSource"></property>
		<property name="typeAliasesPackage" value="com.ssafy.ws.dto"></property>
		<property name="mapperLocations" value="classpath:/mappers/movie.xml"></property>
	</bean>
	
	<!-- myBatis에서 제공하는 scan 태그를 통해 repository interface(DAO)들의 위치 지정 -->
	<mybatis-spring:scan base-package="com.ssafy.ws.model.repo"/>
	
</beans>
